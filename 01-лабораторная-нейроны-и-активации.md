# Лабораторная работа 1: нейроны и функции активации

**Источник:** `Лабораторные.pdf`

## Задача

Реализовать **искусственные нейроны** с различными функциями активации.

---

## Функции активации

Ниже — формулировки из методички (в PDF они могут быть набраны как формулы, а не обычный текст).

### 1. Тождественная функция

$$
f(x) = x
$$

### 2. Единичная ступенька (ступенчатая функция)

$$
f(x) = \begin{cases} 0 & x < 0 \\ 1 & x \ge 0 \end{cases}
$$

### 3. Логистическая (сигмоида, гладкая ступенька)

$$
f(x) = \sigma(x) = \frac{1}{1 + e^{-x}}
$$

### 4. Гиперболический тангенс (th)

$$
f(x) = \operatorname{th}(x) = \frac{e^x - e^{-x}}{e^x + e^{-x}}
$$

### 5. Арктангенс

$$
f(x) = \operatorname{tg}^{-1}(x)
$$

*(в зарубежной литературе часто пишут $\arctan(x)$ — по смыслу то же.)*

### 6. Softsign

$$
f(x) = \frac{x}{1 + |x|}
$$

---

## Нейрон с пороговой функцией активации

### Структура нейрона

**1. Входы**

- $(x_1, x_2, \ldots, x_n)$ — входные сигналы;
- $(w_1, w_2, \ldots, w_n)$ — веса;
- $b$ — **смещение** (bias).

### 1. Сумматор

$$
z = \sum_{i=1}^{n} w_i x_i + b
$$

### 2. Пороговая функция активации (единичная ступенька)

$$
f(z) = \begin{cases} 0 & \text{если } z < 0, \\ 1 & \text{если } z \ge 0 \end{cases}
$$

### 3. Пример работы

**Пусть:**

- веса: $w_1 = 0.6$, $w_2 = -0.4$, $b = 0.3$;
- входы: $x_1 = 1$, $x_2 = 0$.

**Вычисляем:**

$$
z = (0.6 \cdot 1) + (-0.4 \cdot 0) + 0.3 = 0.9
$$

**Активация:**

$$
f(0.9) = 1 \quad \text{(так как } 0.9 \ge 0\text{)}
$$

---

## Что сделать в работе

Реализовать перечисленные выше **функции активации** и модель нейрона (сумматор + выбранная активация) согласно заданию преподавателя.
